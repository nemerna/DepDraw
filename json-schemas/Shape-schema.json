{
    "$id": "./Shape-schema.json",
    "name": "Shape",
    "type": "object",
    "properties": {
        "ID": {
            "type": "integer",
            "description": "The ID of the shape will be the UID of the shape object"
        },
        "ShapeType": {
            "type": "string",
            "description": "a template type which indicates what thing this shape represents\nsuch like pod, svc, deployment etc..",
            "default": "Pod",
            "enum": [
                "Pod",
                "Service",
                "Route",
                "Deployment",
                "Statefulset",
                "Namespace"
            ]
        },
        "ContainedBy": {
            "type": "integer",
            "description": "a reference to the shape ID that is Conatining this shape inside it\nmay be used when creating a NameSpace shape and a pod is being drawed inside this Namespace shape"
        },
        "Name": {
            "type": "string",
            "description": "The Name of the Shape, as the type indicates the type of the k8s corresponding resource, the Name should be a way to differ 2 shapes from the same type one from another in a human readable way"
        },
        "k8s_resource_uri": {
            "type": "string",
            "description": "a reference to the actual k8s resource, which supposed to contain the user customized values",
            "format": "uri"
        }
    },
    "required": [
        "ID",
        "ShapeType",
        "Name",
        "k8s_resource_uri"
    ]
}